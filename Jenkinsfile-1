pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Test') {
       environment {
        HOME="${WORKSPACE}"
      }
      steps {
        sh 'node --version'
      }
    }
  }
}
